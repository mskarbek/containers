---
golang:
  tags:
    - dc02
  stage: build
  rules:
    - changes:
        - jellyfin/**/*
  needs:
    - job: ubi10
      optional: true
  image: $REPO_URL/buildah:latest
  script: |
    buildah login -u $REPO_USERNAME -p $REPO_PASSWORD $REPO_URL
    buildah build -t $REPO_URL/jellyfin --network=host ./jellyfin/
    buildah push $REPO_URL/jellyfin
