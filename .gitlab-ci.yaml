---
build:
  tags:
    - dc02
  stage: build
  image: docker.io/redhat/ubi10:latest
  script: |
    dnf clean all
    dnf -y install buildah fuse-overlayfs
    buildah login -u $REPO_USERNAME -p $REPO_PASSWORD $REPO_URL
    buildah build -t $REPO_URL/buildah .
    buildah push
