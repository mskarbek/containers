---
- hosts: "localhost"
  gather_facts: false
  vars_files:
    - "vars/common.yaml"
    - "vars/main.yaml"
  tasks:
    - ansible.builtin.import_tasks:
        file: "tasks/buildah_create.yaml"
    - ansible.builtin.file:
        dest: "{{ container_path }}{{ item }}"
        state: "touch"
        owner: "root"
        group: "root"
        mode: "0644"
      loop:
        - "/etc/yum.repos.d/pgdg-redhat-all.repo"
        - "/etc/pki/rpm-gpg/RPM-GPG-KEY-PGDG"
      when: "bootstrap"
    - ansible.builtin.copy:
        src: "{{ item.src }}"
        dest: "{{ container_path }}{{ item.dest }}"
        owner: "root"
        group: "root"
        mode: "0644"
      loop:
        - src: "repos/pgdg-redhat-all.repo"
          dest: "/etc/yum.repos.d/pgdg-redhat-all.repo"
        - src: "rpm-gpg/RPM-GPG-KEY-PGDG"
          dest: "/etc/pki/rpm-gpg/RPM-GPG-KEY-PGDG"
      when: "bootstrap"
    - ansible.builtin.command:
        cmd: "rpm --root {{ container_path }} --import {{ container_path }}/etc/pki/rpm-gpg/{{ item }}"
      loop:
        - "RPM-GPG-KEY-PGDG"
      when: "bootstrap"
#    - ansible.builtin.dnf:
#        name:
#          - "postgresql14-server"
#          - "postgresql14-contrib"
#          - "citus_14"
#          - "pg_auto_failover_14"
#          - "pg_qualstats_14"
#          - "pg_stat_kcache_14"
#          - "pg_wait_sampling_14"
#          - "pg_track_settings_14"
#          - "hypopg_14"
#          #- "pgbackrest"
#          #- "powa_14"
#          #- "powa-collector"
#        conf_file: "{{ container_path }}/etc/dnf/dnf.conf"
#        install_weak_deps: false
#        state: "latest"
#        installroot: "{{ container_path }}"
# Ansible DNF module issue:
# https://github.com/ansible/ansible/issues/80644
    - ansible.builtin.command:
        cmd: >
          dnf
          --quiet
          --assumeyes
          --disableplugin="subscription-manager"
          --config={{ container_path }}/etc/dnf/dnf.conf
          --installroot={{ container_path }}
          install
          postgresql14-server
          postgresql14-contrib
          citus_14
          pg_auto_failover_14
          pg_qualstats_14
          pg_stat_kcache_14
          pg_wait_sampling_14
          pg_track_settings_14
          hypopg_14
    - ansible.posix.synchronize:
        src: rootfs/
        dest: "{{ container_path }}/"
        archive: false
        links: true
        recursive: true
        rsync_opts:
          - "--exclude=.gitkeep"
          - "--ignore-existing"
    - ansible.builtin.command:
        cmd: "buildah run --network none {{ container_uuid }} systemctl enable postgresql-14.service"
    - ansible.builtin.command:
        cmd: "buildah config --volume /var/lib/pgsql/14/data {{ container_uuid }}"
    - ansible.builtin.command:
        cmd: "buildah config --volume /var/lib/pgsql/14/backups {{ container_uuid }}"
    - ansible.builtin.import_tasks:
        file: "tasks/buildah_commit.yaml"
