type = process
command = /usr/local/bin/caddy run
command += --config /etc/caddy/Caddyfile
command += --adapter caddyfile
smooth-recovery = true
restart = on-failure
options: runs-on-console
waits-for: caddy-config
